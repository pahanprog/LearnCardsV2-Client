query Deck($id: Float!) {
  deck(deckId: $id) {
    creator {
      username
    }
    title
    description
    canEdit
    learners {
      username
    }
    cards {
      id
      number
      question
      answer
    }
  }
}
